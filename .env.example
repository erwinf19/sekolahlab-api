# Run npm run setup to generate a local .env without overwriting an existing file.
PORT=3000
# Required for login and authenticated routes; random secret, minimum 32 characters.
# Configure separately in Netlify environment variables for Functions.
JWT_SECRET=
